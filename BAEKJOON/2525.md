# 2025-11-21

**🏷️ 정보**
- **플랫폼:** 백준
- **번호:** 2525
- **난이도:** 브론즈 3
- **링크:** [2525](https://www.acmicpc.net/problem/2525)

---

## 💡 문제 이해
> 문제를 나만의 언어로 요약해서 적어보세요.

- 문제: 
	KOI 전자에서는 건강에 좋고 맛있는 훈제오리구이 요리를 간편하게 만드는 인공지능 오븐을 개발하려고 한다. 인공지능 오븐을 사용하는 방법은 적당한 양의 오리 훈제 재료를 인공지능 오븐에 넣으면 된다. 그러면 인공지능 오븐은 오븐구이가 끝나는 시간을 분 단위로 자동적으로 계산한다. 
	
	또한, KOI 전자의 인공지능 오븐 앞면에는 사용자에게 훈제오리구이 요리가 끝나는 시각을 알려 주는 디지털 시계가 있다. 
	
	훈제오리구이를 시작하는 시각과 오븐구이를 하는 데 필요한 시간이 분단위로 주어졌을 때, 오븐구이가 끝나는 시각을 계산하는 프로그램을 작성하시오.
	
- **핵심 조건:** 
	- 시간을 표현해야함으로 60분이 되면 시간을 + 1 한 후 분을 00으로 맞춰줘야 함
	- 23시에서 24시가 아닌 00시로 넘어가야 함

---

## 🚀 접근 방식 (Logic)
> 코드를 짜기 전, 어떤 알고리즘과 자료구조를 쓸지 설계하는 단계입니다.

1. **변수 설정:** 입력받을 `h`, `m`, `p`을 정수로 선언한다. 
2. **조건문 설계:** 
	- `m <= 60`인 경우: `h`를 1증가시키고, `m`의 값에 00을 넣는다.
	- 만약 `h`가 24가 되면 00으로 변경 시킨다.

---

## 💻 정답 코드 (Java)

```java

import java.util.Scanner;

public class Main {

    public static void main(String[] args) {
        Scanner sc = new Scanner(System.in);
        int h, m, plus;
        h = sc.nextInt();
        m = sc.nextInt();
        plus = sc.nextInt();

        if (plus >= 60) { // 더하는 값이 60 이상이면 시/분을 따로 넣어야 함
            h += plus / 60; // 60으로 나눠 시간을 더함
            m += plus % 60; // 60으로 나눈 나머지를 분에 더함
        } else {
            m += plus; // 60보다 작을 경우 바로 분에 넣음
        }

        if (m >= 60) { // 분에 더해져 60보다 커졌을 경우 시간을 1 늘리고 나머지를 분으로 정함
            h++;
            m = m % 60;
        }

        if (h >= 24) { // 24시 이상 커졌을 경우 24를 뺌
            h = h - 24;
        }

        System.out.printf("%d %d", h, m); // 요구사항에 맞게 출력
    }
}
```

---

## 📝 오답 노트 & 회고
> 틀렸다면 왜 틀렸는지, 맞았다면 더 좋은 방법은 없는지 기록하세요.

- **틀린 이유:** -

- **배운 점:** 간단한 조건문을 활용한 예제지만 생각보다 여러 케이스가 생길 수 있어 방심하지 않고 가능한 수를 최대한 생각해서 로직을 짜야한다는 점을 배웠다.

- **다른 사람 풀이 참고:** 

``` java
// 애초에 시간과 분을
m = h * 60 + m //으로 분으로 만든 후 계산한 뒤
h = (m / 60) % 24;
m = m % 60;
// 위와같은 방법으로도 할 수 있다.
```
