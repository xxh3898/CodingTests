# 2025-11-28

**🏷️ 정보**
- **플랫폼:** 백준
- **번호:** 10951
- **난이도:** 브론즈 5
- **링크:** [10951](https://www.acmicpc.net/problem/10951)

---

## 💡 문제 이해
> 문제를 나만의 언어로 요약해서 적어보세요.

- **문제**: 두 정수 A와 B를 입력받은 다음, A+B를 출력하는 프로그램을 작성하시오.

- **핵심 조건:** 입력이 없으면 종료

---

## 🚀 접근 방식 (Logic)
> 코드를 짜기 전, 어떤 알고리즘과 자료구조를 쓸지 설계하는 단계입니다.

1. **변수 설정:** 입력받을 `a`, `b`를 정수로 선언한다. 

---

## 💻 정답 코드 (Java)

```java
import java.io.BufferedReader;
import java.io.BufferedWriter;
import java.io.IOException;
import java.io.InputStreamReader;
import java.io.OutputStreamWriter;
import java.util.StringTokenizer;

public class Main {
    public static void main(String[] args) throws IOException {
        BufferedReader br = new BufferedReader(new InputStreamReader(System.in));
        BufferedWriter bw = new BufferedWriter(new OutputStreamWriter(System.out));
        String input = "";
		
        while ((input = br.readLine()) != null) {
            StringTokenizer st = new StringTokenizer(input);
			
            int a = Integer.parseInt(st.nextToken());
            int b = Integer.parseInt(st.nextToken());
            bw.write((a + b) + "\n");
        }
        bw.flush();
        bw.close();
    }
}
```

---

## 📝 오답 노트 & 회고
> 틀렸다면 왜 틀렸는지, 맞았다면 더 좋은 방법은 없는지 기록하세요.

- **틀린 이유:**`while` 조건문과 내부에서 `br.readLine()`을 중복 호출하여, 홀수 번째 데이터만 읽고 짝수 번째 데이터는 증발함.

- **배운 점:** 
  1. 데이터를 읽어서 변수에 담고, 그게 `null`인지 확인
  2. 조건문에서 읽어둔 변수(`input`)를 재사용해야한다.

- **다른 사람 풀이 참고:** -