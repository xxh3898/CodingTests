---
note_type: coding_test_note
status: archived
created: 2025-11-26
updated: 2025-12-15
platform: boj
problem_id: "11022"
problem_title: "A+B - 8"
difficulty: "브론즈 5"
language: java
category: "반복문"
key_concepts: []
problem_url: "https://www.acmicpc.net/problem/11022"
source_ref:
tags: []
area:
project:
---

# 2025-11-26

## 💡 문제 이해
> 문제를 나만의 언어로 요약해서 적어보세요.

- **문제**: 두 정수 A와 B를 입력받은 다음, A+B를 출력하는 프로그램을 작성하시오.

- **핵심 조건:** 각 테스트 케이스마다 "Case \#x: A + B = C" 형식으로 출력

---

## 🚀 접근 방식 (Logic)
> 코드를 짜기 전, 어떤 알고리즘과 자료구조를 쓸지 설계하는 단계입니다.

1. **변수 설정:** 입력받을 `t`, `a`, `b`를 정수로 선언한다. 

---

## 💻 정답 코드 (Java)

> 백준허브가 자동 생성한 문제 설명과 풀이 코드를 직접 엽니다.

- [[CodingTests/백준/Bronze/11022. A＋B － 8/README|문제 설명 보기]]
- [[CodingTests/백준/Bronze/11022. A＋B － 8/A＋B － 8.java|풀이 코드 보기]]

```java
import java.io.BufferedReader;
import java.io.BufferedWriter;
import java.io.IOException;
import java.io.InputStreamReader;
import java.io.OutputStreamWriter;
import java.util.StringTokenizer;

public class Main {

    public static void main(String[] args) throws IOException {
        BufferedReader br = new BufferedReader(new InputStreamReader(System.in));
        BufferedWriter bw = new BufferedWriter(new OutputStreamWriter(System.out));
        StringTokenizer st;
		
        int t = Integer.parseInt(br.readLine());
        for (int i = 1; i <= t; i++) {
            st = new StringTokenizer(br.readLine());
            int a = (Integer.parseInt(st.nextToken()));
            int b = (Integer.parseInt(st.nextToken()));
            bw.write("Case #" + i + ": " + a + " + " + b + " = " + (a + b) + "\n");
        }
        bw.close();
    }
}
```

---

## 📝 오답 노트 & 회고
> 틀렸다면 왜 틀렸는지, 맞았다면 더 좋은 방법은 없는지 기록하세요.

- **틀린 이유:** -

- **배운 점:** -

- **다른 사람 풀이 참고:** 
  ``` Java
bw.write("Case #"+i+": ");
bw.write(A + " + " + B + " = ");
bw.write((A+B) + "\n");
```
한 번에 출력하지 않고 나눠서 출력하였다.