# 2025-11-28

**🏷️ 정보**
- **플랫폼:** 백준
- **번호:** 2439
- **난이도:** 브론즈 4
- **링크:** [2439](https://www.acmicpc.net/problem/2439)

---

## 💡 문제 이해
> 문제를 나만의 언어로 요약해서 적어보세요.

- **문제**: 첫째 줄에는 별 1개, 둘째 줄에는 별 2개, N번째 줄에는 별 N개를 찍는 문제
  하지만, 오른쪽을 기준으로 정렬한 별(예제 참고)을 출력하시오.

- **핵심 조건:** N - 1 만큼 공백이 있어야함

---

## 🚀 접근 방식 (Logic)
> 코드를 짜기 전, 어떤 알고리즘과 자료구조를 쓸지 설계하는 단계입니다.

1. **변수 설정:** 입력받을 `n`를 정수로 선언한다. 

---

## 💻 정답 코드 (Java)

```java
import java.io.BufferedReader;
import java.io.BufferedWriter;
import java.io.IOException;
import java.io.InputStreamReader;
import java.io.OutputStreamWriter;

public class Main {

    public static void main(String[] args) throws IOException {
        BufferedReader br = new BufferedReader(new InputStreamReader(System.in));
        BufferedWriter bw = new BufferedWriter(new OutputStreamWriter(System.out));

        int n = Integer.parseInt(br.readLine());
        for (int i = 1; i <= n; i++) {
            for (int j = 1; j <= n - i; j++) {
                bw.write(" ");
            }
            for (int j = 1; j <= i; j++) {
                bw.write("*");
            }
            bw.write("\n");
        }
        bw.close();
    }
}
```

---

## 📝 오답 노트 & 회고
> 틀렸다면 왜 틀렸는지, 맞았다면 더 좋은 방법은 없는지 기록하세요.

- **틀린 이유:** 시작 반복문을 `for (int i = 1; i <= t; i++)`로 설정했더니 i가 0이라 별은 출력되지 않고 개행만 되어 출력형식이 잘못되어 틀림

- **배운 점:** 행(`i`)이 증가할수록 공백(`n-i`)은 줄어들고, 별(`i`)은 늘어나는 것

- **다른 사람 풀이 참고:** -