# 2025-11-30

**🏷️ 정보**
- **플랫폼:** 백준
- **번호:** 10818
- **난이도:** 브론즈 3
- **링크:** [10818](https://www.acmicpc.net/problem/10818)

---

## 💡 문제 이해
> 문제를 나만의 언어로 요약해서 적어보세요.

- **문제**: N개의 정수가 주어진다. 이때, 최솟값과 최댓값을 구하는 프로그램을 작성하시오.

- **핵심 조건:** 첫째 줄에 주어진 정수 N개의 최솟값과 최댓값을 공백으로 구분해 출력한다.

---

## 🚀 접근 방식 (Logic)
> 코드를 짜기 전, 어떤 알고리즘과 자료구조를 쓸지 설계하는 단계입니다.

1. **변수 설정:** 입력받을 `n`을 정수로 선언한다. 

2. **조건문 설계:** 
	- `if (num < min) {min = num;}`
	- `if (max < num) {max = num;}`

---

## 💻 정답 코드 (Java)

```java
import java.io.BufferedReader;
import java.io.BufferedWriter;
import java.io.IOException;
import java.io.InputStreamReader;
import java.io.OutputStreamWriter;
import java.util.StringTokenizer;

public class Main {

    public static void main(String[] args) throws IOException {
        BufferedReader br = new BufferedReader(new InputStreamReader(System.in));
        BufferedWriter bw = new BufferedWriter(new OutputStreamWriter(System.out));
		
        StringTokenizer st = new StringTokenizer(br.readLine());
        int n = Integer.parseInt(st.nextToken());
        int max = -1000000, min = 1000000;
		
        st = new StringTokenizer(br.readLine());
		
        for (int i = 0; i < n; i++) {
            int num = Integer.parseInt(st.nextToken());
			
            if (num < min) {
                min = num;
            }
            if (max < num) {
                max = num;
            }
        }
        bw.write(min + " ");
        bw.write(max + "");
		
        bw.flush();
        bw.close();
        br.close();
    }
}
```

---

## 📝 오답 노트 & 회고
> 틀렸다면 왜 틀렸는지, 맞았다면 더 좋은 방법은 없는지 기록하세요.

- **틀린 이유:** max 초기값을 0으로 설정해 음수 대응 오류

- **배운 점:** -

- **다른 사람 풀이 참고:** `Arrays.sort()`메소드를 사용하여 배열을 정렬한 후  첫 번째와 마지막 수 출력