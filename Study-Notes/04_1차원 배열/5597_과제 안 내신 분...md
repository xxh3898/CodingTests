---
note_type: coding_test_note
status: archived
created: 2025-12-04
updated: 2025-12-15
platform: boj
problem_id: "5597"
problem_title: "과제 안 내신 분.."
difficulty: "브론즈 3"
language: java
category: "1차원 배열"
key_concepts: []
problem_url: "https://www.acmicpc.net/problem/5597"
source_ref:
tags: []
area:
project:
---

# 2025-12-03

## 💡 문제 이해
> 문제를 나만의 언어로 요약해서 적어보세요.

- **문제**: 
  X대학 M교수님은 프로그래밍 수업을 맡고 있다. 교실엔 학생이 30명이 있는데, 학생 명부엔 각 학생별로 1번부터 30번까지 출석번호가 붙어 있다.
  
  교수님이 내준 특별과제를 28명이 제출했는데, 그 중에서 제출 안 한 학생 2명의 출석번호를 구하는 프로그램을 작성하시오.

- **핵심 조건:** 출력은 2줄이다. 1번째 줄엔 제출하지 않은 학생의 출석번호 중 가장 작은 것을 출력하고, 2번째 줄에선 그 다음 출석번호를 출력한다.

---

## 🚀 접근 방식 (Logic)
> 코드를 짜기 전, 어떤 알고리즘과 자료구조를 쓸지 설계하는 단계입니다.

1. **변수 설정:** 입력받을 `arr`을 정수형 배열로 선언한다. 

2. **조건문 설계:** 
	- `if (arr[k] == j) {count = 1;}` 1~30까지 돌려서 배열 안에 숫자가 있다면 `count`를 1로 변경하여 확인

3. **시간 복잡도:** -

---

## 💻 정답 코드 (Java)

> 백준허브가 자동 생성한 문제 설명과 풀이 코드를 직접 엽니다.

- [[CodingTests/백준/Bronze/5597. 과제 안 내신 분．．？/README|문제 설명 보기]]
- [[CodingTests/백준/Bronze/5597. 과제 안 내신 분．．？/과제 안 내신 분．．？.java|풀이 코드 보기]]

```java
import java.io.BufferedReader;
import java.io.BufferedWriter;
import java.io.IOException;
import java.io.InputStreamReader;
import java.io.OutputStreamWriter;

public class Main {

    public static void main(String[] args) throws IOException {
        BufferedReader br = new BufferedReader(new InputStreamReader(System.in));
        BufferedWriter bw = new BufferedWriter(new OutputStreamWriter(System.out));
		
        int[] arr = new int[28];
        int[] outArr = new int[2];
        int n = 0;
        for (int i = 0; i < arr.length; i++) {
            arr[i] = Integer.parseInt(br.readLine());
	    }
		
        for (int j = 1; j <= 30; j++) {
            int count = 0;
			
            for (int k = 0; k < arr.length; k++) {
                if (arr[k] == j) {
                    count = 1;
                }
            }
            if (count == 0) {
                outArr[n] = j;
                n++;
            }
        }
        bw.write(outArr[0] + " " + outArr[1]);
        bw.flush();
        bw.close();
        br.close();
    }
}
```

---

## 📝 오답 노트 & 회고
> 틀렸다면 왜 틀렸는지, 맞았다면 더 좋은 방법은 없는지 기록하세요.

- **틀린 이유:** -

- **배운 점:** flag 변수를 count 대신 boolean을 사용하여 의마가 훨씬 더 명확해지게 할 수 있다.

- **다른 사람 풀이 참고:** `for(int i=1; i<29; i++) {int success = sc.nextInt();student[success] = 1;}for(int i=1; i<student.length; i++) {if(student[i]!=1)System.out.println(i);}`
