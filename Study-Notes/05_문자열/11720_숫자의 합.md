---
note_type: coding_test_note
status: archived
created: 2025-11-28
updated: 2025-12-15
platform: boj
problem_id: "11720"
problem_title: "숫자의 합"
difficulty: "브론즈 4"
language: java
category: "문자열"
key_concepts: []
problem_url: "https://www.acmicpc.net/problem/11720"
source_ref:
tags: []
area:
project:
---

# 2025-12-11

## 💡 문제 이해
> 문제를 나만의 언어로 요약해서 적어보세요.

- **문제**: N개의 숫자가 공백 없이 쓰여있다. 이 숫자를 모두 합해서 출력하는 프로그램을 작성하시오.

- **핵심 조건:** 입력으로 주어진 숫자 N개의 합을 출력한다.

---

## 🚀 접근 방식 (Logic)
> 코드를 짜기 전, 어떤 알고리즘과 자료구조를 쓸지 설계하는 단계입니다.

1. **변수 설정:** 입력받을 `n`을 정수로 선언한다. 

2. **조건문 설계:**  -

3. **시간 복잡도:** 단순 조건문이므로 O(n) 예상.

---

## 💻 정답 코드 (Java)

> 백준허브가 자동 생성한 문제 설명과 풀이 코드를 직접 엽니다.

- [[CodingTests/백준/Bronze/11720. 숫자의 합/README|문제 설명 보기]]
- [[CodingTests/백준/Bronze/11720. 숫자의 합/숫자의 합.java|풀이 코드 보기]]

```java
import java.io.BufferedReader;
import java.io.BufferedWriter;
import java.io.IOException;
import java.io.InputStreamReader;
import java.io.OutputStreamWriter;
import java.util.StringTokenizer;

public class Main {

    public static void main(String[] args) throws IOException {
        BufferedReader br = new BufferedReader(new InputStreamReader(System.in));
        BufferedWriter bw = new BufferedWriter(new OutputStreamWriter(System.out));
		
        int n = Integer.parseInt(br.readLine());
		
        StringTokenizer st = new StringTokenizer(br.readLine());
        String str = st.nextToken();
		
        int sum = 0;
        for (int i = 0; i < n; i++) {
            sum += str.charAt(i) - '0';
        }
		
        bw.write("" + sum);
        br.close();
        bw.close();
    }
}
```

---

## 📝 오답 노트 & 회고
> 틀렸다면 왜 틀렸는지, 맞았다면 더 좋은 방법은 없는지 기록하세요.

- **틀린 이유:** 정수로 변환하려다 실패함

- **배운 점:** 아스키코드 0을 빼면 원하는 자연수가 나온다.

- **다른 사람 풀이 참고:** `for(byte value : br.readLine().getBytes()) {sum += (value - '0');}`