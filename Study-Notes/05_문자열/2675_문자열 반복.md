---
note_type: coding_test_note
status: archived
created: 2025-12-13
updated: 2025-12-15
platform: boj
problem_id: "2675"
problem_title: "문자열 반복"
difficulty: "브론즈 2"
language: java
category: "문자열"
key_concepts: []
problem_url: "https://www.acmicpc.net/problem/2675"
source_ref:
tags: []
area:
project:
---

# 2025-12-13

## 💡 문제 이해
> 문제를 나만의 언어로 요약해서 적어보세요.

- **문제**: 
  문자열 S를 입력받은 후에, 각 문자를 R번 반복해 새 문자열 P를 만든 후 출력하는 프로그램을 작성하시오. 즉, 첫 번째 문자를 R번 반복하고, 두 번째 문자를 R번 반복하는 식으로 P를 만들면 된다. S에는 QR Code "alphanumeric" 문자만 들어있다.
  
  QR Code "alphanumeric" 문자는 `0123456789ABCDEFGHIJKLMNOPQRSTUVWXYZ\$%*+-./:` 이다.

- **핵심 조건:** 각 테스트 케이스에 대해 P를 출력한다.

---

## 🚀 접근 방식 (Logic)
> 코드를 짜기 전, 어떤 알고리즘과 자료구조를 쓸지 설계하는 단계입니다.

1. **변수 설정:** 입력받을 `n`을 정수로, `str`을 문자열로 선언한다. 

2. **조건문 설계:** -

3. **시간 복잡도:** -

---

## 💻 정답 코드 (Java)

> 백준허브가 자동 생성한 문제 설명과 풀이 코드를 직접 엽니다.

- [[CodingTests/백준/Bronze/2675. 문자열 반복/README|문제 설명 보기]]
- [[CodingTests/백준/Bronze/2675. 문자열 반복/문자열 반복.java|풀이 코드 보기]]

```java
import java.io.BufferedReader;
import java.io.BufferedWriter;
import java.io.IOException;
import java.io.InputStreamReader;
import java.io.OutputStreamWriter;
import java.util.StringTokenizer;

public class Main {

    public static void main(String[] args) throws IOException {
        BufferedReader br = new BufferedReader(new InputStreamReader(System.in));
        BufferedWriter bw = new BufferedWriter(new OutputStreamWriter(System.out));

        int n = Integer.parseInt(br.readLine());
        StringTokenizer st;
		
        for (int i = 0; i < n; i++) {
            st = new StringTokenizer(br.readLine());
			
            int N = Integer.parseInt(st.nextToken());
            String str = st.nextToken();
			
            int l = str.length();
			
            for (int k = 0; k < l; k++) {
                for (int j = 0; j < N; j++) {
                    bw.write(str.charAt(k));
                }
            }
            bw.write("\n");
        }
        br.close();
        bw.close();
    }
}
```

---

## 📝 오답 노트 & 회고
> 틀렸다면 왜 틀렸는지, 맞았다면 더 좋은 방법은 없는지 기록하세요.

- **틀린 이유:** (-

- **배운 점:** -

- **다른 사람 풀이 참고:** -