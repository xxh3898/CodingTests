# 2026-03-17
## 💡 문제 이해
> $N$개의 수가 주어졌을 때, 이를 오름차순으로 정렬하는 프로그램을 작성합니다. ($N \le 10,000,000$)

정수 $N$개가 주어지면 이를 오름차순으로 정렬해서 출력하는 문제.
이 문제는 이전 문제(2751번)보다 데이터의 양이 훨씬 많으며($N=10^7$), 시간 제한과 메모리 제한이 매우 엄격합니다.

- **핵심 조건**
    - $N \le 10,000,000$ (천만 개)
    - 시간 제한: 3초
    - 메모리 제한: 512MB (본 시스템 기준 메모리 사용량: 약 384MB)
    - **빠른 입출력 필수**: 데이터의 개수가 방대하므로 `Scanner` 보다는 무조건 `BufferedReader`와 `BufferedWriter`를 사용해야 합니다.

---

## 🚀 접근 방식 (Logic)
> 빠른 입출력(Fast I/O) 패턴을 적용

1. **변수 설정:** 
    - `int n`: 입력받을 숫자의 개수.
    - `long[] arr`: 숫자를 저장할 배열. (천만 개의 데이터를 담기 위해 선언)
2. **입출력 설계:** 
    - `BufferedReader`를 사용하여 라인 단위로 빠르게 입력받음.
    - `BufferedWriter`를 사용하여 출력 버퍼를 효율적으로 관리하며 출력 성능 극대화.
3. **정렬 알고리즘:** 
    - `Arrays.sort()`를 사용하여 $O(N \log N)$ 속도로 정렬 수행.
4. **시간 복잡도:** 
    - $O(N \log N)$이지만 상수가 작은 `Arrays.sort`와 빠른 입출력 덕분에 제한 시간 내에 통과 가능.

---

## 💻 소스 코드 및 문제 설명
> 백준허브가 자동 생성한 **문제 폴더**로 이동합니다.

- [📂 문제 풀이 폴더 열기 (소스코드 & README)](../../백준/Bronze/10989. 수 정렬하기 3)

```java
import java.io.BufferedReader;
import java.io.BufferedWriter;
import java.io.IOException;
import java.io.InputStreamReader;
import java.io.OutputStreamWriter;
import java.util.Arrays;

public class Main {
    public static void main(String[] args) throws IOException {
        BufferedReader br = new BufferedReader(new InputStreamReader(System.in));
        BufferedWriter bw = new BufferedWriter(new OutputStreamWriter(System.out));

        int n = Integer.parseInt(br.readLine());

        long[] arr = new long[n];

        for (int i = 0; i < arr.length; i++) {
            arr[i] = Long.parseLong(br.readLine());
        }

        Arrays.sort(arr);

        for (long i : arr) {
            bw.write(i + "\n");
        }

        bw.close();
        br.close();
    }
}
```

---

## 📝 오답 노트 & 회고
> 한 번에 맞춘 성과와 배운 점을 기록합니다.

- **틀린 이유:** 
    - -
- **배운 점:** 
    - 2751번 문제에서 겪었던 시행착오 덕분에, 대량 데이터를 다룰 때 **`BufferedReader`와 `BufferedWriter`의 조합이 얼마나 필수적인지** 몸소 체감했습니다.
    - 입출력 방식만 최적화해도 정렬 알고리즘의 성능을 충분히 뒷받침할 수 있다는 점을 확인했습니다.
- **다른 사람 풀이 참고:** 
    - 이 문제는 보통 메모리 제한이 매우 작게 설정되는 경우가 많아, `long[]` 배열을 직접 생성하기보다 입력값의 범위가 작을 때 사용하는 **카운팅 정렬(Counting Sort)**을 권장하기도 합니다. (메모리 사용량을 획기적으로 줄일 수 있음)