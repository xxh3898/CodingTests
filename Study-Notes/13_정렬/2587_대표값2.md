# 2026-03-13
## 💡 문제 이해
> 문제를 나만의 언어로 요약해서 적어보세요.

- **핵심 조건**:  
	- 서로 다른 5개의 자연수가 입력된다.  
	 - 입력된 수들의 **평균값**과 **중앙값**을 출력해야 한다.  
	- 평균은 모든 수의 합을 5로 나눈 값이다.  
	- 중앙값은 정렬했을 때 **가운데 값(3번째 값)**이다.

---

## 🚀 접근 방식 (Logic)
> 코드를 짜기 전, 어떤 알고리즘과 자료구조를 쓸지 설계하는 단계입니다.

1. **변수 설정:**  
	  - `int[] arr = new int[5]` → 입력된 숫자 저장  
	- `int sum = 0` → 평균 계산을 위한 합

2. **조건문 설계:**  
	- 평균 출력: `sum / 5`  
	- 중앙값 출력: 배열 정렬 후 `arr[2]`

3. **정렬 방식:**  
	- 이중 반복문을 사용한 **버블 정렬**

4. **시간 복잡도 계산:**
	 버블 정렬 비교 횟수
	- 바깥 반복문: `n = 5`
	- 안쪽 반복문: `n - 1 = 4`

   총 비교 횟수

   ```
   5 × 4 = 20
   ```

   일반적인 시간 복잡도

   ```
   O(n²)
   ```

   그러나 이 문제는 입력 크기가 **5로 고정**이므로 실제 성능 문제는 발생하지 않는다.

---

## 💻 소스 코드 및 문제 설명
> 백준허브가 자동 생성한 **문제 폴더**로 이동합니다.

- [📂 문제 풀이 폴더 열기 (소스코드 & README)](../../백준/Bronze/2587. 대표값2)

---

## 📝 오답 노트 & 회고
> 틀렸다면 왜 틀렸는지, 맞았다면 더 좋은 방법은 없는지 기록하세요.

- **틀린 이유:**  
  없음. 한 번에 정답 처리.

- **배운 점:**  
	- 중앙값은 **정렬 후 가운데 인덱스**를 사용하면 쉽게 구할 수 있다.  
	- 작은 입력 데이터에서는 **버블 정렬** 같은 단순 정렬도 충분히 사용 가능하다.

- **다른 사람 풀이 참고:**  
	- `Arrays.sort(arr)`를 사용하면 정렬 코드를 더 간단하게 구현할 수 있다.